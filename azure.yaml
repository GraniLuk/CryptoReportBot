# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: cryptoreportbot
metadata:
  template: cryptoreportbot@0.0.1-beta
infra:
  provider: bicep
services:
  web:
    project: ./src/CryptoReportBot
    language: dotnet
    host: appservice
    docker:
      path: ./prod.dockerfile
      context: ./
